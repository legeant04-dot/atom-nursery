<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- INTERNET is the only permission this app declares.
       Location is NOT requested here on purpose. A TWA renders inside Chrome, so the website asks
       Chrome for location exactly as it does in the browser today, and Chrome asks the user. Adding
       ACCESS_FINE_LOCATION would put a second, pointless permission prompt in front of every parent
       at install time and grant this shell nothing it can use. -->
  <uses-permission android:name="android.permission.INTERNET" />

  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:allowBackup="true"
      android:supportsRtl="true">

    <!-- The whole app. LauncherActivity comes from androidbrowserhelper; it opens the URL below as a
         Trusted Web Activity and, if the two-sided handshake verifies, with no address bar. -->
    <activity
        android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
        android:exported="true"
        android:theme="@style/Theme.Atom.Splash">

      <meta-data
          android:name="android.support.customtabs.trusted.DEFAULT_URL"
          android:value="@string/launch_url" />

      <!-- Match the web app's own theme, so the system bars do not flash a different colour on the
           way in. These are the same values as manifest.json (#1565C0 / #F4F7FB). -->
      <meta-data
          android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
          android:resource="@color/colorPrimary" />
      <meta-data
          android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
          android:resource="@color/backgroundColor" />

      <meta-data
          android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
          android:resource="@drawable/splash" />
      <meta-data
          android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
          android:resource="@color/backgroundColor" />
      <meta-data
          android:name="android.support.customtabs.trusted.FILE_PROVIDER_AUTHORITY"
          android:value="${applicationId}.fileprovider" />

      <!-- The app's half of the domain handshake. The site's half is assetlinks.json at the origin
           root; both must name the same package and the same signing fingerprint. -->
      <meta-data
          android:name="asset_statements"
          android:resource="@string/asset_statements" />

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Opening the school's own links inside the app rather than bouncing out to a browser tab.
         Scoped to the app's own path: without android:path this would offer to open every page on
         legeant04-dot.github.io, including anything unrelated that ever gets published there. -->
    <activity-alias
        android:name="LauncherActivityAlias"
        android:targetActivity="com.google.androidbrowserhelper.trusted.LauncherActivity"
        android:exported="true">
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="legeant04-dot.github.io"
            android:pathPrefix="/atom-nursery" />
      </intent-filter>
    </activity-alias>

    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/filepaths" />
    </provider>

  </application>
</manifest>
