# dependencies (73MB — never commit)
node_modules/
tools/node_modules/

# clasp local credentials (NEVER commit) — .clasp.json (scriptId) is OK to keep
.clasprc.json

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# throwaway / temp tooling
tools/_*.js
*.zip
_pchi/

# local pre-shrink copies of the image assets (the real originals live in git history,
# e.g. `git show d133896:webapp/assets/logo.png`) — see dist/shrink_assets.py
dist/assets_original/

# generated by tools/build_web.js — the minified copy CI publishes (webapp/ is the source)
dist_pages/
